# @zoom/meetingsdk pins an exact React 18.2.0 peer (for its Client View bundle),
# but this app is on React 18.3.x and only uses the prebuilt Component View
# (@zoom/meetingsdk/embedded), which doesn't share the host app's React.
# Without this, a plain `npm install` fails with ERESOLVE.
legacy-peer-deps=true
